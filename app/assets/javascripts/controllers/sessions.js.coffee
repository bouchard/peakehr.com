$ ->

  $(document).on 'page:load', ->

    # if $('body').hasClass('sessions')
    #   $('.register-link').on 'click.sessions', ->
    #     $('.sign-in-container').slideUp(100, ->
    #       $('.register-container').slideDown(100)
    #     )
    #     return false

  $(document).on 'page:receive', ->

    # $('.register-link').off 'click.sessions'
